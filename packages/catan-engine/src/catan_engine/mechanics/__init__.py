"""Game logic: the single-game, traceable/vmappable rule helpers (placement,
awards, trade, dice, robber, setup, development) and the action layer
(``action``) that composes them into the engine's 15 vectorized transitions."""
